const assert = require('chai').assert;
import { formatDuplicatesCheckTableData } from '../';

describe('formatDuplicatesCheck', () => {
  it('passes', () => {
    assert(true, true, 'istrue');
  });
});
