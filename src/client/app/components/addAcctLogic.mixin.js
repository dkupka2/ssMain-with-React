
// add account logic

// let prevAcctsList = flatAccts()
// let newAccts = {}
// newState.acctInput = ""
// if (prevAcctsList.includes(action.payload)) return state
// // if accts contains accounts, copy accts' enumerable
// if (prevAcctsList.length > 0) Object.assign(newAccts, state.accts)
// newAccts[action.payload] = {}
// newState.accts = newAccts
// newState.acctSelected = action.payload
// console.log("should return new state", newState)
// return returnState(newState)